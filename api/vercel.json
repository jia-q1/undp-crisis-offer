{
  "installCommand": "cd .. && npm install",
  "buildCommand": "cd .. && npm run build --workspace=shared && npm run build --workspace=api",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
