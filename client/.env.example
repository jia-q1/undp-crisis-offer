# Only needed in production, where the API is a separate Vercel project.
# Leave unset locally — Vite's dev proxy (vite.config.ts) handles /api requests instead.
VITE_API_URL=https://your-api-project.vercel.app
